id: sign-new-contract
title: 签新合同
metadata:
  tags: [contract, core]
  priority: P0

steps:
  - if: { condition: asset.exists }
    then: search-or-create-asset
    else: create-asset
  - if: { condition: tenant.exists }
    then: search-or-create-tenant
    else: create-tenant
  - task: create-contract
  - task: issue-receipt
