# Application mode: sqlite or postgres
MODE=sqlite

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=
DB_NAME=asset_leasing

# JWT (required)
JWT_SECRET=change-me-to-a-random-secret

# Admin seed password (default: admin123 if not set)
ADMIN_PASSWORD=

# Server
PORT=8080
